

if (true)
{
    let a=1;
    const b=2;
    var c=9;
}
//console.log(a);        //let does not work outside of scope which is good-->**block scope
//console.log(b);  //const does not work outside of scope which is good
console.log(c);  //var scope er baire kaj kore which is not good --> ***global scope